# 🚀 GitHub Pages 部署完整指南

## ✅ 已为您配置好所有文件！

所有代码已经适配 GitHub Pages，可以直接上传部署！

---

## 📦 **已修改/创建的文件：**

### 1. ✅ **vite.config.ts** - 已配置相对路径
- 添加了 `base: './'` 适用于所有 GitHub Pages 仓库

### 2. ✅ **.github/workflows/deploy.yml** - 自动部署脚本（新建）
- 每次推送代码到 main 分支自动构建部署

### 3. ✅ **.nojekyll** - 禁用 Jekyll（新建）
- 告诉 GitHub Pages 不要使用 Jekyll 处理

### 4. ✅ **package.json** - 添加部署脚本
- 添加了 `deploy` 命令（可选使用）

### 5. ✅ **.gitignore** - Git 忽略文件（新建）
- 忽略 node_modules、dist 等文件

### 6. ✅ **styles/globals.css** - 修复 CSS 导入顺序
- @import 已移到最前面

---

## 🎯 **部署方式（推荐使用方式 1）：**

---

## 📤 **方式 1：自动部署（推荐）** ⭐

使用 GitHub Actions 自动部署，最简单！

### **步骤 1：上传代码到 GitHub**

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit for GitHub Pages"

# 关联远程仓库（替换成您的仓库地址）
git remote add origin https://github.com/您的用户名/仓库名.git

# 推送到 main 分支
git push -u origin main
```

### **步骤 2：在 GitHub 启用 Pages**

1. **打开您的 GitHub 仓库**
2. **点击 Settings（设置）**
3. **在左侧菜单找到 Pages**
4. **在 "Source" 下选择：**
   - Source: **GitHub Actions**（不是 Deploy from a branch）
5. **点击 Save**

### **步骤 3：等待自动部署**

1. **点击 Actions 标签**
2. **会看到 "Deploy to GitHub Pages" 工作流正在运行**
3. **等待绿色勾号（大约 1-2 分钟）**
4. **部署成功后，访问：**
   ```
   https://您的用户名.github.io/仓库名/
   ```

### **✅ 完成！**

以后每次推送代码到 main 分支，GitHub 会自动重新部署！

---

## 🔧 **方式 2：手动部署（备选）**

如果您不想使用 GitHub Actions：

### **步骤 1：本地构建**

```bash
# 安装依赖
npm install

# 构建
npm run build
```

### **步骤 2：推送 dist 到 gh-pages 分支**

```bash
# 安装 gh-pages 工具
npm install -D gh-pages

# 部署
npm run deploy
```

### **步骤 3：在 GitHub 设置 Pages**

1. 进入仓库 Settings → Pages
2. Source 选择：**Deploy from a branch**
3. Branch 选择：**gh-pages** 分支，目录选 **/ (root)**
4. 点击 Save

---

## 📋 **重要说明：**

### **关于仓库名和访问地址：**

**情况 1：仓库名是 `username.github.io`**
- 访问地址：`https://username.github.io/`
- 无需修改配置，已支持

**情况 2：仓库名是其他名字（如 `portfolio`）**
- 访问地址：`https://username.github.io/portfolio/`
- 无需修改配置，已使用相对路径

**情况 3：自定义域名**
- 在 Settings → Pages → Custom domain 设置
- 需要配置 DNS CNAME 记录

---

## 🔍 **检查部署是否成功：**

### **成功标志：**

1. ✅ **Actions 标签显示绿色勾号**
2. ✅ **Settings → Pages 显示网址：**
   ```
   Your site is live at https://username.github.io/repo-name/
   ```
3. ✅ **访问网址能看到完整网站**
4. ✅ **中英文切换正常**
5. ✅ **所有页面和组件正常显示**

### **如果遇到问题：**

#### **问题 1：Actions 失败**

**查看错误：**
1. 进入 Actions 标签
2. 点击失败的工作流
3. 展开红色的步骤查看错误

**常见原因：**
- ❌ Pages 未启用 → 去 Settings → Pages 启用
- ❌ 权限不足 → 去 Settings → Actions → General → Workflow permissions，选择 "Read and write permissions"

#### **问题 2：页面空白**

**检查：**
1. F12 打开开发者工具
2. 查看 Console 是否有错误
3. 检查 Network 是否有 404 错误

**解决：**
- 如果 CSS/JS 文件 404，可能是路径问题
- 确认 vite.config.ts 中 `base: './'` 已设置

#### **问题 3：刷新页面 404**

**原因：** GitHub Pages 不支持 SPA 路由

**解决方案：**
- 目前配置使用 hash 路由（URL 带 #）
- 已在代码中配置，无需修改

---

## 🎉 **快速开始清单：**

部署前确认：

- [ ] ✅ 所有代码已提交到本地 Git
- [ ] ✅ 已关联 GitHub 远程仓库
- [ ] ✅ 已推送到 main 分支
- [ ] ✅ GitHub Settings → Pages 已启用
- [ ] ✅ Source 选择 "GitHub Actions"
- [ ] ✅ Actions 工作流运行成功（绿色勾号）
- [ ] ✅ 访问网址能看到网站

---

## 📞 **下一步操作：**

### **立即执行（推荐方式 1）：**

```bash
# 1. 添加所有文件
git add .

# 2. 提交
git commit -m "Configure for GitHub Pages deployment"

# 3. 推送到 GitHub（替换成您的仓库地址）
git remote add origin https://github.com/您的用户名/仓库名.git
git push -u origin main
```

### **然后：**

1. 进入 GitHub 仓库 Settings → Pages
2. Source 选择 **GitHub Actions**
3. 等待 Actions 部署完成
4. 访问您的网站！

---

## 🌐 **部署成功后：**

您的网站将在以下地址访问：

```
https://您的GitHub用户名.github.io/仓库名/
```

例如：
- 用户名：`zhangsan`
- 仓库名：`portfolio`
- 网址：`https://zhangsan.github.io/portfolio/`

---

## 💡 **额外提示：**

### **更新网站：**

以后想更新网站，只需：

```bash
# 修改代码后
git add .
git commit -m "Update content"
git push
```

GitHub Actions 会自动重新部署！

### **本地预览：**

部署前想本地测试：

```bash
# 开发模式（热更新）
npm run dev

# 构建后预览（和生产环境一样）
npm run build
npm run preview
```

---

## 🆘 **需要帮助？**

如果遇到问题，请提供：

1. **GitHub 仓库地址**
2. **Actions 错误日志**（如果失败）
3. **浏览器控制台错误**（如果页面有问题）
4. **Settings → Pages 的截图**

---

## ✅ **现在开始上传吧！**

所有代码已经准备好，直接推送到 GitHub 就能自动部署！🚀✨

**祝部署顺利！** 🎉
