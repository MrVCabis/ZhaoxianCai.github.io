# ⚡ GitHub Pages 快速部署指令

## 🚀 三步部署（复制粘贴即可）：

### **步骤 1：推送代码**

```bash
# 添加所有文件
git add .

# 提交
git commit -m "Deploy to GitHub Pages"

# 首次推送（替换成您的仓库地址）
git remote add origin https://github.com/您的用户名/仓库名.git
git push -u origin main

# 如果已经关联过仓库，直接推送：
git push
```

---

### **步骤 2：启用 GitHub Pages**

1. 打开 GitHub 仓库
2. 点击 **Settings**
3. 左侧点击 **Pages**
4. **Source** 选择：**GitHub Actions**
5. 点击 **Save**

---

### **步骤 3：等待部署完成**

1. 点击仓库的 **Actions** 标签
2. 等待绿色勾号 ✅（约 1-2 分钟）
3. 访问：`https://您的用户名.github.io/仓库名/`

---

## ✅ 完成！

以后每次更新，只需：

```bash
git add .
git commit -m "Update"
git push
```

GitHub 会自动重新部署！

---

## 📍 网站地址格式：

- **仓库名是 `username.github.io`** 
  → `https://username.github.io/`

- **仓库名是其他（如 `portfolio`）**
  → `https://username.github.io/portfolio/`

---

## 🔧 如果 Actions 失败：

**检查权限：**

1. Settings → Actions → General
2. Workflow permissions
3. 选择 **"Read and write permissions"**
4. 勾选 **"Allow GitHub Actions to create and approve pull requests"**
5. Save

**重新运行：**

1. Actions 标签
2. 点击失败的工作流
3. 点击 **"Re-run all jobs"**

---

## 📦 已配置的文件：

✅ `vite.config.ts` - 相对路径配置  
✅ `.github/workflows/deploy.yml` - 自动部署  
✅ `.nojekyll` - 禁用 Jekyll  
✅ `.gitignore` - Git 忽略文件  
✅ `styles/globals.css` - 修复 CSS 顺序  

**所有文件已准备好，直接上传即可！**

---

## 🎯 本地测试命令：

```bash
# 开发模式（热更新）
npm run dev

# 构建预览（和生产环境一样）
npm run build
npm run preview
```

---

**现在就开始上传吧！** 🚀✨
